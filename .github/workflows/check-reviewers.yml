on:
  pull_request_target:
    types: [unlocked]

name: TEST

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: TEST
        run: echo unlocked
