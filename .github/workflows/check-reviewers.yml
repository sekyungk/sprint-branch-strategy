on:
  pull_request_target:
    types: [unlocked]

name: Staging Deploy to Amazon S3

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: TEST
        run: echo unlocked
